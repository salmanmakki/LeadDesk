{
  "buildCommand": "npm install --legacy-peer-deps --prefix client && npm run build:client",
  "outputDirectory": "client/dist",
  "installCommand": "npm install --legacy-peer-deps",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 10,
      "memory": 256
    }
  }
}
